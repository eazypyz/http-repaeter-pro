name = "http-repeater-pro"
main = "worker.js"
compatibility_date = "2024-11-01"

# Free plan Cloudflare Workers sudah cukup untuk proyek ini.
# Deploy dengan:
#   npx wrangler deploy
#
# Setelah deploy, catat URL worker (contoh: https://http-repeater-pro.<subdomain>.workers.dev)
# dan masukkan ke frontend/app.js pada konstanta WORKER_URL.
